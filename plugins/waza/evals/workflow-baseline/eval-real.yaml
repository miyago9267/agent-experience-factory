name: agent-workflow-baseline-real
description: Generic behavior checks using Waza's real executor
skill: workflow-baseline
schemaVersion: "1.2"
version: "0.2.0"
config:
  executor: copilot-sdk
  model: auto
  trials_per_task: 2
  timeout_seconds: 120
  parallel: false
metrics:
  - name: workflow_contract
    weight: 1.0
    threshold: 0.8
tasks:
  - "tasks/*.yaml"
