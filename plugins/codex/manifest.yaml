schema_version: "1"
kind: agent-workflow-plugin
plugin_id: codex
version: 0.1.0
plugin_type: runtime_adapter
runtime: codex
entrypoint: ../runtime-adapter.sh
capabilities:
  - context_bootstrap
  - result_contract
  - capability_gap_reporting
healthcheck:
  command: ["--health"]
